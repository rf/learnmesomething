%h2 New Question

= form_for([@user, @request]) do |f| 
  %ul
    - @request.errors.full_messages.each do |error|
      %li
        #{error}

  = f.label :title
  = f.text_field :title
  
  = f.label :description
  = f.text_area :description

  = f.submit "Submit"

